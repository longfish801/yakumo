#! washscr call03

## slice
# call テスト
#-combi #行番号付与
#-first #前処理
#-last #後処理

## map
# dec
	import io.github.longfish801.shared.util.TextUtil;
# args
	List<String> lines
# closure 行番号付与
	return TextUtil.addLineNo(lines);
# closure 前処理
	return [ '▼ここから', *lines ];
# closure 後処理
	return [ *lines, '  ▲ここまで' ];
