#! clmap テスト３
## dec
	import org.apache.commons.lang3.StringUtils;

## map map1
# args
	String arg

# closure setter
	config.test = arg;

# closure getter1
	return "${config.test}-1-${arg}";

# closure getter
	config.test = arg;
	return clmap.cl(arg).call('test');

## map map2
# args
	String arg

# closure
	return "${config.test}-1-${arg}";

# closure getter2
	return "${config.test}-2-${arg}";

