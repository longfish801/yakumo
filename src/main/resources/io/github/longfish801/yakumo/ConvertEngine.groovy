
// クロージャマップ
clmap {
	combiKey = '';	// コンビキー
}

// テンプレート
template {
	defaultKey = 'default';	// デフォルトキー
}

// 現在の設定の保存用ファイル名
current {
	washscr = 'washscr.tpac';	// 事前整形スクリプト
	clmap = 'clmap.tpac';	// クロージャマップ
	meta = 'meta.txt';	// メタ定義
}
